pipeline {
    agent any

    stages {
        stage('Test Maven') {
            steps {
                script {
                    def mvnHome = tool 'Maven'
                    sh "${mvnHome}/bin/mvn --version"
                }
            }
        }
    }
}
